<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@mipmap/splash_xhdpi">

    <com.google.android.gms.common.SignInButton
        android:id="@+id/sign_in_google_btn"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:visibility="visible"
        android:layout_above="@+id/sign_in_facebook_btn"

        android:layout_marginBottom="16dp"
        android:layout_alignStart="@+id/sign_in_facebook_btn"
        android:layout_alignEnd="@+id/sign_in_facebook_btn" />

    <com.facebook.login.widget.LoginButton
        android:id="@+id/sign_in_facebook_btn"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:visibility="visible" />

</RelativeLayout>
